<div class = "row">
	<div class = "col-md-6 col-md-offset-3">
		<%= form_for [@topic, @reply]  do |f| %>
		  
			<%= f.label :content, "Add Comment" %>
			<%= f.text_field :content %>
		 
			<%= f.submit  "Create Reply", data: {disable_with: "Creating..."} %>
		  
		<% end %>
	</div>
</div>
