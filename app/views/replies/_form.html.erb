<div class = "row">
	<div class = "col-md-6 col-md-offset-3">
		<%= form_for [@topic, @reply]  do |f| %>
		  
			<%= f.label :content %>
			<%= f.text_field :content, class: "form-control" %>
		 
				<%= f.submit  "Create Reply", class: "btn btn-primary", data: {disable_with: "Creating..."} %> 
		
		  
		<% end %>
	</div>
</div>
