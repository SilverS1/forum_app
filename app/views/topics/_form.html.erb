<div class = "row">
	<div class = "col-md-6 col-md-offset-3">
		<%= form_for [@subforum, @topic]  do |f| %>

			<%= f.label :title %>
			<%= f.text_field :title %>
		  
			<%= f.label :content %>
			<%= f.text_field :content %>
		 
			<%= f.submit  "Submit", data: {disable_with: "Creating..."} %>
		  
		<% end %>
	</div>
</div>
